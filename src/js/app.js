var hotemoji = document.querySelectorAll("price.hot");
hotemoji.innerHTML = hotemoji.innerHTML +"🔥"


window.addEventListener("DOMContentLoaded", () => {
  // This block will be executed once the page is loaded and ready


});
